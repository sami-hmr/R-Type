name: CI - Development

on:
  push:
    branches:
    - windows*

  pull_request:
    branches:
    - windows*

jobs:
  ci:
    uses: ./.github/workflows/reusable-ci.yml
    with:
      auto-format: false
      run-windows: true
    secrets: inherit